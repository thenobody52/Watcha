# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# Reducing memory footprint significantly.
org.gradle.jvmargs=-Xmx512m -XX:MaxMetaspaceSize=256m
# Disable daemon, already handled by --no-daemon but good to ensure.
org.gradle.daemon=false
# Parallel builds can consume too much memory.
org.gradle.parallel=false
# Enable build caching for faster rebuilds.
org.gradle.caching=true
org.gradle.configuration-cache=true
# Set workers to 1 to avoid OOM.
org.gradle.workers.max=1
# Kotlin compiler execution strategy to in-process.
kotlin.compiler.execution.strategy=in-process
# Enable pass-through for missing google-services.json if needed.
googleServices.missing.passthrough=true
